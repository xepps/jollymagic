define(
    "jollymagic/controller",
    [
    ],

    function () {
        "use strict";

        var Controller = function (page) {
            this.page = page;
        };

        Controller.prototype.init = function () {
        };

        return Controller;
    }
);
